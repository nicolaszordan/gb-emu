[package]
name = "emu"
version = "0.1.0"
edition = "2024"

[features]
test-utilities = []

[dependencies]

[dev-dependencies]
emu = { path = ".", features = ["test-utilities"] } # this allows us to write doc tests that use the test utilities

[lints]
workspace = true

