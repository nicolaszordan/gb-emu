[package]
name = "gb"
version = "0.1.0"
edition = "2024"

[dependencies]
emu = { path = "../emu" }

[dev-dependencies]
emu = { path = "../emu", features = ["test-utilities"] }

[lints]
workspace = true

