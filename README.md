# GB Emu

GameBoy emulators written in Rust with frontends planned for Web, Mobile and Desktop.

## Structure

- `emulators/`: Emulator implementations.
- `files/`: Docs, test ROMs, and other files.
- `frontends/`: Emulator frontends (eg. CLI, Mobile, Web).
- `tools/`: Tools for development and testing.
